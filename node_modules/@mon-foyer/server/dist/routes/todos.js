"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const index_1 = require("../db/index");
const push_1 = require("../utils/push");
const router = (0, express_1.Router)();
router.get('/', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { assignedTo, status, priority } = req.query;
    let q = 'SELECT * FROM todos WHERE 1=1';
    const params = [];
    if (assignedTo) {
        q += ' AND assigned_to = ?';
        params.push(assignedTo);
    }
    if (status) {
        q += ' AND status = ?';
        params.push(status);
    }
    if (priority) {
        q += ' AND priority = ?';
        params.push(priority);
    }
    q += " ORDER BY CASE priority WHEN 'high' THEN 0 WHEN 'normal' THEN 1 ELSE 2 END, due_date ASC";
    res.json(await db.all(q, ...params));
});
router.post('/', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { title, description, priority, assignedTo, status, dueDate, eventId } = req.body;
    const r = await db.run('INSERT INTO todos (title,description,priority,assigned_to,status,due_date,event_id,created_at) VALUES (?,?,?,?,?,?,?,?)', title, description || null, priority || 'normal', assignedTo || null, status || 'pending', dueDate || null, eventId || null, new Date().toISOString());
    const todo = await db.get('SELECT * FROM todos WHERE id = ?', r.lastID);
    if (assignedTo) {
        (0, push_1.sendPushToMember)(assignedTo, { title: '✅ Nouvelle tâche assignée', body: title, url: '/?tab=todo' }).catch(() => { });
    }
    else {
        (0, push_1.sendPushToAll)({ title: '✅ Nouvelle tâche', body: title, url: '/?tab=todo' }).catch(() => { });
    }
    res.status(201).json(todo);
});
router.put('/:id', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { title, description, priority, assignedTo, status, dueDate, eventId } = req.body;
    await db.run('UPDATE todos SET title=?,description=?,priority=?,assigned_to=?,status=?,due_date=?,event_id=? WHERE id=?', title, description || null, priority, assignedTo || null, status, dueDate || null, eventId || null, req.params.id);
    res.status(200).json(await db.get('SELECT * FROM todos WHERE id = ?', req.params.id));
});
router.patch('/:id/status', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { status } = req.body;
    await db.run('UPDATE todos SET status=? WHERE id=?', status, req.params.id);
    res.json(await db.get('SELECT * FROM todos WHERE id = ?', req.params.id));
});
router.delete('/:id', async (req, res) => {
    const db = await (0, index_1.getDb)();
    await db.run('DELETE FROM todos WHERE id=?', req.params.id);
    res.json({ success: true });
});
exports.default = router;
