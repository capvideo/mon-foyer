"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const index_1 = require("../db/index");
const push_1 = require("../utils/push");
const router = (0, express_1.Router)();
router.get('/vapid-public', (_req, res) => {
    res.json({ key: (0, push_1.getVapidPublicKey)() });
});
router.post('/subscribe', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { memberId, subscription } = req.body;
    const { endpoint, keys } = subscription;
    await db.run('DELETE FROM push_subscriptions WHERE member_id=? AND endpoint=?', memberId, endpoint);
    await db.run('INSERT INTO push_subscriptions (member_id,endpoint,p256dh,auth,created_at) VALUES (?,?,?,?,?)', memberId, endpoint, keys.p256dh, keys.auth, new Date().toISOString());
    res.json({ success: true });
});
router.delete('/unsubscribe', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { memberId } = req.body;
    await db.run('DELETE FROM push_subscriptions WHERE member_id=?', memberId);
    res.json({ success: true });
});
exports.default = router;
