"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = require("express");
const jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
const index_1 = require("../db/index");
const auth_1 = require("../middleware/auth");
const push_1 = require("../utils/push");
const router = (0, express_1.Router)();
router.get('/', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { month, memberId } = req.query;
    let q = 'SELECT * FROM events WHERE 1=1';
    const params = [];
    if (month) {
        q += ' AND date LIKE ?';
        params.push(`${month}%`);
    }
    if (memberId) {
        q += ' AND member_ids LIKE ?';
        params.push(`%"${memberId}"%`);
    }
    q += ' ORDER BY date ASC, time ASC';
    const rows = await db.all(q, ...params);
    res.json(rows.map((e) => ({ ...e, memberIds: JSON.parse(e.member_ids) })));
});
// Must come before /:id to avoid "calendar.ics" being matched as an ID.
// Auth via ?token= query param so native calendar apps (webcal://) can subscribe.
router.get('/calendar.ics', async (req, res) => {
    const token = req.query.token || req.headers.authorization?.replace('Bearer ', '');
    if (!token) {
        res.status(401).send('Token requis');
        return;
    }
    try {
        jsonwebtoken_1.default.verify(token, auth_1.JWT_SECRET);
    }
    catch {
        res.status(401).send('Token invalide ou expiré');
        return;
    }
    const db = await (0, index_1.getDb)();
    const { memberId } = req.query;
    let q = 'SELECT * FROM events WHERE 1=1';
    const params = [];
    if (memberId) {
        q += ' AND member_ids LIKE ?';
        params.push(`%"${memberId}"%`);
    }
    q += ' ORDER BY date ASC, time ASC';
    const rows = await db.all(q, ...params);
    const toIcalDt = (date, time) => time ? `${date.replace(/-/g, '')}T${time.replace(':', '')}00` : null;
    const vevents = rows.map((e) => {
        const dtstart = toIcalDt(e.date, e.time);
        const dtend = toIcalDt(e.end_date || e.date, e.end_time || e.time);
        const lines = [
            'BEGIN:VEVENT',
            `UID:${e.id}@monfoyer`,
            dtstart ? `DTSTART:${dtstart}` : `DTSTART;VALUE=DATE:${e.date.replace(/-/g, '')}`,
            dtend ? `DTEND:${dtend}` : `DTEND;VALUE=DATE:${(e.end_date || e.date).replace(/-/g, '')}`,
            `SUMMARY:${e.title.replace(/\n/g, '\\n')}`,
            e.description ? `DESCRIPTION:${e.description.replace(/\n/g, '\\n')}` : '',
            e.location ? `LOCATION:${e.location}` : '',
            'END:VEVENT',
        ].filter(Boolean);
        return lines.join('\r\n');
    });
    const ics = [
        'BEGIN:VCALENDAR',
        'VERSION:2.0',
        'PRODID:-//Mon Foyer//FR',
        'CALSCALE:GREGORIAN',
        'METHOD:PUBLISH',
        'X-WR-CALNAME:Mon Foyer',
        'X-WR-TIMEZONE:Europe/Paris',
        ...vevents,
        'END:VCALENDAR',
    ].join('\r\n');
    res.setHeader('Content-Type', 'text/calendar; charset=utf-8');
    res.setHeader('Content-Disposition', 'attachment; filename="mon-foyer.ics"');
    res.setHeader('Cache-Control', 'no-cache, no-store');
    res.send(ics);
});
router.get('/:id', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const e = await db.get('SELECT * FROM events WHERE id = ?', req.params.id);
    if (!e)
        return res.status(404).json({ error: 'Événement non trouvé' });
    res.json({ ...e, memberIds: JSON.parse(e.member_ids) });
});
router.post('/', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { title, date, time, endDate, endTime, description, memberIds, color, location, reminderMinutes } = req.body;
    const r = await db.run('INSERT INTO events (title,date,time,end_date,end_time,description,member_ids,color,location,reminder_minutes) VALUES (?,?,?,?,?,?,?,?,?,?)', title, date, time || null, endDate || null, endTime || null, description || null, JSON.stringify(memberIds || []), color || '#378ADD', location || null, reminderMinutes || null);
    const e = await db.get('SELECT * FROM events WHERE id = ?', r.lastID);
    (0, push_1.sendPushToAll)({ title: '📅 Nouvel événement', body: `${title} — ${date}`, url: '/?tab=agenda' }).catch(() => { });
    res.status(201).json({ ...e, memberIds: JSON.parse(e.member_ids) });
});
router.put('/:id', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const { title, date, time, endDate, endTime, description, memberIds, color, location, reminderMinutes } = req.body;
    await db.run('UPDATE events SET title=?,date=?,time=?,end_date=?,end_time=?,description=?,member_ids=?,color=?,location=?,reminder_minutes=? WHERE id=?', title, date, time || null, endDate || null, endTime || null, description || null, JSON.stringify(memberIds || []), color || '#378ADD', location || null, reminderMinutes || null, req.params.id);
    const e = await db.get('SELECT * FROM events WHERE id = ?', req.params.id);
    (0, push_1.sendPushToAll)({ title: '📅 Événement modifié', body: `${title}`, url: '/?tab=agenda' }).catch(() => { });
    res.json({ ...e, memberIds: JSON.parse(e.member_ids) });
});
router.delete('/:id', async (req, res) => {
    const db = await (0, index_1.getDb)();
    await db.run('DELETE FROM events WHERE id=?', req.params.id);
    res.json({ success: true });
});
router.get('/:id/ics', async (req, res) => {
    const db = await (0, index_1.getDb)();
    const e = await db.get('SELECT * FROM events WHERE id = ?', req.params.id);
    if (!e)
        return res.status(404).json({ error: 'Non trouvé' });
    const dtstart = e.date.replace(/-/g, '') + (e.time ? 'T' + e.time.replace(':', '') + '00' : '');
    const dtend = e.end_date
        ? e.end_date.replace(/-/g, '') + (e.end_time ? 'T' + e.end_time.replace(':', '') + '00' : '')
        : dtstart;
    const ics = [
        'BEGIN:VCALENDAR', 'VERSION:2.0', 'PRODID:-//Mon Foyer//FR',
        'BEGIN:VEVENT',
        `UID:${e.id}@monfoyer`,
        `DTSTART:${dtstart}`, `DTEND:${dtend}`,
        `SUMMARY:${e.title}`,
        e.description ? `DESCRIPTION:${e.description}` : '',
        e.location ? `LOCATION:${e.location}` : '',
        'END:VEVENT', 'END:VCALENDAR',
    ].filter(Boolean).join('\r\n');
    res.setHeader('Content-Type', 'text/calendar');
    res.setHeader('Content-Disposition', `attachment; filename="${e.title}.ics"`);
    res.send(ics);
});
exports.default = router;
