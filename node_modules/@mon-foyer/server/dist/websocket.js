"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.setupWebSocket = setupWebSocket;
const ws_1 = require("ws");
const index_1 = require("./db/index");
const clients = new Set();
function setupWebSocket(wss) {
    wss.on('connection', (ws) => {
        const client = { ws, memberId: 'unknown' };
        clients.add(client);
        ws.on('message', async (raw) => {
            try {
                const msg = JSON.parse(raw.toString());
                const db = await (0, index_1.getDb)();
                switch (msg.type) {
                    case 'auth':
                        client.memberId = msg.memberId;
                        client.channelId = msg.channelId;
                        ws.send(JSON.stringify({ type: 'auth_ok', memberId: msg.memberId }));
                        break;
                    case 'join_channel':
                        client.channelId = msg.channelId;
                        ws.send(JSON.stringify({ type: 'joined', channelId: msg.channelId }));
                        break;
                    case 'message': {
                        const { channelId, memberId, content, msgType, metadata } = msg;
                        const r = await db.run('INSERT INTO messages (channel_id,member_id,content,type,timestamp,metadata) VALUES (?,?,?,?,?,?)', channelId, memberId, content, msgType || 'text', new Date().toISOString(), metadata ? JSON.stringify(metadata) : null);
                        const saved = await db.get('SELECT * FROM messages WHERE id = ?', r.lastID);
                        const broadcast = JSON.stringify({ type: 'message', data: saved });
                        for (const c of clients) {
                            if (c.channelId === channelId && c.ws.readyState === ws_1.WebSocket.OPEN) {
                                c.ws.send(broadcast);
                            }
                        }
                        break;
                    }
                    case 'typing': {
                        const { channelId, memberId } = msg;
                        const broadcast = JSON.stringify({ type: 'typing', channelId, memberId });
                        for (const c of clients) {
                            if (c.channelId === channelId && c.ws !== ws && c.ws.readyState === ws_1.WebSocket.OPEN) {
                                c.ws.send(broadcast);
                            }
                        }
                        break;
                    }
                    case 'ping':
                        ws.send(JSON.stringify({ type: 'pong' }));
                        break;
                }
            }
            catch (e) {
                console.error('WS error:', e);
            }
        });
        ws.on('close', () => clients.delete(client));
        ws.send(JSON.stringify({ type: 'connected' }));
    });
}
